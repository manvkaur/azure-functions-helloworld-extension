parameters:
  - name: publishToNugetOrg
    type: boolean
  - name: artifactName
    type: string
    default: drop

resources:
  repositories:
    - repository: 1es
      type: git
      name: 1ESPipelineTemplates/1ESPipelineTemplates
      ref: refs/tags/release
    - repository: eng
      type: git
      name: engineering
      ref: refs/tags/release

extends:
  template: v1/1ES.Official.PipelineTemplate.yml@1es
  parameters:
    pool:
      name: 1es-pool-azfunc
      image: 1es-windows-2022
      os: windows

    stages:
      - stage: Prepare
        jobs:
          - job: Prepare
            templateContext:
              type: validationJob

            steps:
              - download: build
                artifact: ${{ parameters.artifactName }}
                patterns: '**/Microsoft.Azure.Functions.Extensions.HelloWorld.*.nupkg'
                displayName: Download host extension package
              - template: /eng/ci/templates/steps/get-package-version.yml@self
                parameters:
                  packageName: Microsoft.Azure.Functions.Extensions.HelloWorld
                  buildTag: host-extension
                  dropPath: $(Pipeline.Workspace)/build/${{ parameters.artifactName }}

      - stage: Release
        dependsOn: Prepare

        jobs:
          - template: /ci/release-nuget-package.yml@eng
            parameters:
              isProduction: true
              approvers: '[TEAM FOUNDATION]\Azure Functions Extensions'
              stagingFeed: public/pre-release
              packages: '**/Microsoft.Azure.Functions.Extensions.HelloWorld.*.nupkg'
              artifact:
                name: ${{ parameters.artifactName }}
                pipeline: build
              ${{ if eq(parameters.publishToNugetOrg, true) }}:
                partnerDrop:
                  serviceConnection: azure-sdk-partner-drops
                  targetFolder: azure-functions/dotnet/helloworld-extension/host/$(Build.BuildNumber)
