<Project>

  <Import Project="$(MSBuildThisFileDirectory)RepositoryInfo.targets" />
  <Import Project="$(MSBuildThisFileDirectory)Release.targets" />
  <Import Project="$(MSBuildThisFileDirectory)WebJobsReference.targets" />

</Project>
